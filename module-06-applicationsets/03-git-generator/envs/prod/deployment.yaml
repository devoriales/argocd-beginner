# One directory per environment. The Git generator turns each of these into an
# Application automatically, so adding an environment is adding a folder.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web-prod
  template:
    metadata:
      labels:
        app: web-prod
    spec:
      containers:
        - name: web
          image: nginx:1.29-alpine
